const theme = {
    background: 'red',
    palette: {
        type: 'light',
    },
    overrides: {
        MuiAppBar: {
            root: {
                background: '#fff'
            }
        }
    }
};

export default theme;